/**
 * multi line paragraph not any more messup with + string concate
 */

 let paragraph = `This is my
                  paragraph. 
                  Multi new line string`;

console.log( `${paragraph}` );